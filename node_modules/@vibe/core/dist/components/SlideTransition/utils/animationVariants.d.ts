import { SlideDirection } from "../SlideTransition.types";
export declare const slideAnimationVariants: {
    initial: (direction: SlideDirection) => {
        x: string;
    };
    enter: {
        x: number;
    };
};
export declare const slideAnimationTransition: {
    duration: number;
    ease: number[];
};
