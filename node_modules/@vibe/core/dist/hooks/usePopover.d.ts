/// <reference types="react" />
import { Placement } from "./popoverConstants";
export default function usePopover(referenceElement: HTMLElement, popperElement: HTMLElement, { isOpen, placement, observeContentResize }: {
    isOpen?: boolean;
    placement?: Placement;
    observeContentResize?: boolean;
}): {
    styles: {
        [key: string]: import("react").CSSProperties;
    };
    attributes: {
        [key: string]: {
            [key: string]: string;
        };
    };
};
