import { z } from 'zod';
import { ManifestFeatureSchema } from '../../services/schemas/manifest-service-schemas.js';
import { AppId, AppVersionId } from '../general/index.js';
export type ManifestFeature = z.infer<typeof ManifestFeatureSchema>;
export type AppCreateCommandTasksContext = {
    appId?: AppId;
    appName?: string;
    appVersionId?: AppVersionId;
    features?: ManifestFeature[];
    githubUrl: string;
    folder: string;
    branch: string;
    targetPath: string;
};
