import { BaseCommand } from '../../commands-base/base-command.js';
export default class ApiGenerate extends BaseCommand {
    DEBUG_TAG: string;
    static description: string;
    run(): Promise<void>;
    runSetupApi(): Promise<void>;
}
